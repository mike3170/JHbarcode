<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.stit.jhbarcode">
    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".PicklingFinishedQueryActivity"></activity>
        <activity android:name=".PlatingReceptionQueryActivity" />
        <activity android:name=".MatReturnQueryActivity" />
        <activity android:name=".JobOrderPickupQueryActivity" />
        <activity android:name=".LocationMoveQueryActivity" />
        <activity android:name=".TestActivity" />
        <activity android:name=".PicklingFinishedActivity" />
        <activity android:name=".PlatingReceptionActivity" />
        <activity android:name=".JobOrderPickupActivity" />
        <activity android:name=".MatReturnActivity" />
        <activity android:name=".LocationMoveActivity" />
        <activity android:name=".tracer.JobOrderTracerActivity" />
        <activity android:name=".tracer.PicklingFinishedTracerActivity" />
        <activity
            android:name=".LoginActivity"
            android:label="@string/app_name"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar" />
    </application>

</manifest>